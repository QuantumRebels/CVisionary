# Environment variables for the Orchestrator Agent Service

# --- FastAPI/Uvicorn --- #
# ORCHESTRATOR_PORT=8004 # Port for the orchestrator service (uvicorn will use this if CMD is adapted)

# --- OpenAI Configuration --- #
OPENAI_API_KEY="your_openai_api_key_here"
# AGENT_LLM_MODEL="gpt-4o" # Or gpt-3.5-turbo-0125, etc.
# AGENT_LLM_TEMPERATURE=0.0
# AGENT_MEMORY_WINDOW_K=5

# --- Gemini Configuration --- #
GEMINI_API_KEY="your_google_gemini_api_key_here"

# --- Service URLs for dependent CVisionary microservices --- #
# Ensure these URLs are accessible from where the orchestrator service is running (e.g., within Docker network)
EMBEDDING_SERVICE_URL="http://localhost:8000/embed" # Or http://embedding_service:8000/embed if using docker-compose service names
# GENERATOR_SERVICE_URL handles both generation and retrieval tasks.
# Example assumes /generate for generation and /retrieve (or similar) for retrieval on this service.
GENERATION_SERVICE_URL="http://localhost:8001/" # Or http://generator_service:8001/ - Note: port changed to 8001 assuming it takes over retrieval's port or has distinct endpoints.
SCORING_SERVICE_URL="http://localhost:8003/score"     # Or http://scoring_service:8003/score

# --- Agent Logic Parameters --- #
MATCH_THRESHOLD=0.75 # Score threshold below which suggestions are triggered

# --- Logging --- #
# LOG_LEVEL="INFO" # Example, if you implement more granular log control
